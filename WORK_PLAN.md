# 다온 낙상 케어 — 온라인 전시 제출 작업 지시서

**제출일: 2026-08-17(월) / 실질 마감: 8/16(일)**
새 채팅을 시작할 때 §0을 먼저 붙여넣고, 그다음 해당 작업 섹션을 통째로 붙여넣으면 됩니다.

---

## §0. 공통 컨텍스트 (매 채팅 맨 앞에 붙일 것)

```
프로젝트: fall_detection_DL (/Users/Yeon/Desktop/fall_detection_DL)
브랜치: feature/daon-web-platform

구성:
- 백엔드: FastAPI (webservice/), SQLite (webservice/daon.db)
- 프런트: React + TypeScript + Vite (webservice/frontend/), 빌드 결과를 FastAPI가 서빙
- CV 파이프라인 1 — 실시간 낙상 감지: main.py (MediaPipe Pose + sklearn 분류기 + 아두이노 타일)
- CV 파이프라인 2 — 컨설팅 분석: webservice/consulting/ (YOLO11 사람 검출 → 히트맵 → 규칙 기반 소견)

목표: '온라인 전시 체험관' 출품. 웹 브라우저에서 바로 열리는 URL을 제출해야 하고,
전시 사이트가 그 URL을 iframe으로 임베드합니다. 반응형 필수.

시연 구조 (확정):
- 부스 현장: 실시간 낙상 감지 + 타일 발사 하드웨어 시연에 집중
- 온라인 URL: 컨설팅 기능을 AI 생성 할머니 영상 3개(안방/거실/부엌)로 체험.
  실시간 감지 페이지는 "연결 가능한 카메라 없음 → 부스 방문 안내 + 데모 영상"으로 대체

제약:
- mediapipe는 0.10.14 고정 (신버전이 mp.solutions.pose 제거함)
- face_recognition은 웹 서비스에서 안 씀 (main.py --face-every 전용, 기본 꺼짐)
```

---

## 작업 A. 컨설팅 분석 속도 최적화

**우선순위: 1 / 예상 0.5일 / 담당: Claude**

### 문제

`webservice/consulting/analyze.py`의 `analyze_video()`가 영상의 **모든 프레임**에
**yolo11m** 을 `model.track()` 으로 돌립니다. CPU에서 30초·30fps 영상이면 900프레임
→ 5~15분. 온라인 체험에서 이 대기시간은 치명적입니다.

### 지시문

```
webservice/consulting/analyze.py 의 분석 속도를 최적화해줘.

1) 먼저 baseline을 측정해. test_videos/ 안의 영상 하나로 현재 analyze_video()가
   몇 초 걸리는지 재고 기록해줘. 최적화 후 비교할 기준이 필요해.

2) 다음 네 가지를 적용해:
   - 프레임 샘플링: _iter_frames()에 stride 인자를 추가해서 N장당 1장만
     yield하게 해. 기본값은 원본 fps를 읽어서 "초당 3프레임"이 되도록 계산.
     동선 히트맵에 30fps는 불필요해.
   - 모델 교체: yolo11m.pt → yolo11n.pt. AI 생성 영상에 사람이 1명뿐이라
     nano로 충분해. model_name 기본값을 바꾸고 환경변수로 덮어쓸 수 있게.
   - 입력 해상도: predict 호출에 imgsz=384 추가.
   - track() → predict(): 사람이 1명이라 ID 추적이 불필요해. 단, 작업 B에서
     궤적을 이어야 하니 "프레임 순서대로 박스 리스트"는 그대로 유지해야 해.

3) 목표는 30초 영상 기준 3~5초. 달성 못 하면 stride와 imgsz를 더 조여봐.

4) frames_to_heatmap()은 순수 함수라 tests/에서 YOLO 없이 테스트되고 있어.
   이 계약을 깨지 마. 작업 후 pytest 전체를 돌려서 통과 확인해줘.
```

### 완료 기준

- [ ] baseline / 최적화 후 소요시간이 숫자로 기록됨
- [ ] 30초 영상 분석이 5초 이내
- [ ] `pytest` 전체 통과
- [ ] 위험 구역 판정 결과가 최적화 전과 크게 어긋나지 않음 (눈으로 확인)

### 주의

- `ultralytics`는 지연 임포트 구조입니다. 무거운 torch를 테스트에서 강제하지 않도록
  일부러 그렇게 짜둔 것이니 최상단으로 끌어올리지 마세요.
- `yolo11n.pt`는 첫 실행 시 자동 다운로드됩니다. `.gitignore`에 `*.pt`가 있습니다.

---

## 작업 B. 체류 → 동선(이동 경로) 분석으로 전환

**우선순위: 1 / 예상 0.5일 / 담당: Claude / 작업 A와 함께 진행**

### 문제

`webservice/consulting/heatmap.py`의 `accumulate_heatmap()`이 **bbox 전체를 채워서**
누적합니다. 그래서 "가만히 서 있으면 뜨거워지는" = 체류 시간 지도가 나옵니다.
`rules.py`의 문구도 "머무는 시간이 가장 깁니다"로 되어 있습니다.

우리가 원하는 컨설팅 기준은 **자주 지나다니는 동선**입니다.

### 지시문

```
컨설팅 분석을 '오래 머무는 곳' 기준에서 '자주 지나다니는 동선' 기준으로 바꿔줘.

1) webservice/consulting/heatmap.py
   - bbox 전체를 채우는 대신 '발끝 좌표'를 쓴다. bbox 하단 중앙
     ((x1+x2)/2, y2) 이 사람이 실제로 딛고 선 바닥 위치야.
   - 프레임 순서대로 발끝 좌표를 모아 궤적(trajectory)을 만든다.
   - 격자 셀별로 '통과 횟수'를 센다. 핵심: 연속된 프레임이 같은 셀에 있으면
     1회로 묶어야 해. 셀이 바뀌는 순간에만 +1. 이래야 체류가 아니라
     이동 빈도가 측정돼.
   - 기존 accumulate_heatmap()은 지우지 말고 남겨둬 (테스트가 참조 중).
     새 함수 accumulate_path() / path_cell_counts() 를 추가하는 방향으로.

2) webservice/consulting/rules.py
   - grid_scores()가 체류 히트맵 대신 통과 횟수 격자를 받도록.
   - _RECOMMEND 문구를 동선 기준으로 다시 써:
     "높음" → 이 구역을 가장 자주 지나갑니다. 통행량이 많은 만큼 걸림 요소
              제거와 손잡이 설치가 우선입니다.
     "보통" → 이동이 관찰된 구역입니다. 문턱·전선 등을 점검하세요.
     "낮음" → 통행이 드문 구역입니다.
   - summary 문구의 "체류가 가장 두드러졌으며"도 동선 표현으로 교체.
   - 임계값 0.66 / 0.33 은 통과 횟수 분포에 맞게 재조정 필요. 실제 영상으로
     찍어보고 정해줘.

3) 기존 테스트가 깨지면 새 함수용 테스트를 추가하고, 기존 테스트는
   accumulate_heatmap()을 계속 커버하도록 유지해.
```

### 완료 기준

- [ ] 같은 자리에 오래 서 있는 영상 → 통과 횟수가 낮게 나옴
- [ ] 방을 여러 번 왕복하는 영상 → 해당 경로 셀이 높게 나옴
- [ ] 리포트 문구에 "머무는" 표현이 남아 있지 않음
- [ ] `pytest` 전체 통과

---

## 작업 C. 경로 시각화 렌더러

**우선순위: 2 / 예상 0.3일 / 담당: Claude / 작업 B 이후**

### 지시문

```
webservice/consulting/heatmap.py 의 render_hazard_boxes()를 확장해서,
방 사진 위에 이동 경로를 선으로 그려줘.

- 발끝 궤적을 cv2.polylines 로 그린다. 시작점에서 끝점으로 갈수록 색이
  변하거나 굵기가 변하면 방향을 읽을 수 있어서 좋아.
- 그 위에 기존처럼 위험 구간을 반투명 박스 + 굵은 테두리로 덮는다.
- 프레임이 흔들리는 영상이면 궤적이 튈 수 있으니 좌표에 간단한
  이동평균(window 3~5) 스무딩을 넣어줘.
- 기존 render_hazard_boxes()의 시그니처를 깨지 말고 새 함수로 추가하거나
  선택 인자로 받아. routes_consulting.py 의 호출부도 함께 수정.
```

### 왜 하는가

히트맵 얼룩보다 **"이 분은 이 경로로 다닙니다"** 라는 선 하나가 심사위원·관람객에게
훨씬 잘 읽힙니다. 컨설팅 근거를 시각적으로 설명해주는 요소이기도 합니다.

---

## 작업 D. 배포 — 최대 리스크

**우선순위: 1 / 예상 1.5~2일 / 8/7~8/9**

### 왜 최대 리스크인가

FastAPI + torch + ultralytics + opencv 조합은 이미지가 크고 메모리를 먹습니다.
무료 티어(Render Free 512MB)에는 **들어가지 않습니다.** 마지막 날 배포하다 막히면
제출 자체가 불가능해집니다.

### 지시문

```
이 프로젝트를 웹에 배포해서 공개 URL을 만들어줘. 전시 사이트가 이 URL을
iframe으로 임베드할 거야.

1) 의존성 다이어트
   - requirements.txt 에서 face_recognition==1.3.0 제거. 웹 서비스는 안 쓰는데
     dlib를 소스 빌드해서 배포를 망가뜨려. main.py 전용이므로
     requirements-local.txt 같은 별도 파일로 분리해줘.
   - torch를 CPU 전용 wheel로 설치하도록 고정
     (--extra-index-url https://download.pytorch.org/whl/cpu).
     이미지가 2.5GB → 800MB 수준으로 줄어.
   - opencv-contrib-python → opencv-python-headless 로 교체 검토.
     서버에는 GUI가 없어서 headless가 더 가볍고 안전해. main.py의 cv2.imshow는
     로컬 전용이니 로컬 requirements에만 GUI 버전을 남겨.

2) Dockerfile 작성
   - 멀티스테이지: node로 webservice/frontend 빌드 → python 런타임에 dist 복사
   - yolo11n.pt 를 빌드 시점에 미리 받아두기 (첫 요청 때 다운로드하면 타임아웃)
   - CMD: uvicorn webservice.app:app --host 0.0.0.0 --port $PORT

3) iframe 허용
   - webservice/app.py 에 응답 헤더 미들웨어 추가:
     X-Frame-Options 는 아예 설정하지 말고,
     Content-Security-Policy: frame-ancestors * 를 넣어.
   - 세션 쿠키가 iframe 안에서 죽지 않도록 SessionMiddleware에
     same_site="none", https_only=True 설정. 이거 빠뜨리면 iframe 안에서
     로그인이 안 돼.

4) 호스팅
   - RAM 1~2GB 인스턴스가 필요해. Railway 또는 Render 유료(월 $5~7) 기준으로 진행.
   - SQLite를 쓰므로 파일 시스템이 재시작 때 날아가는지 확인하고, 날아간다면
     영구 볼륨을 붙이거나 시작 시 seed를 다시 돌리도록.
   - DAON_SECRET 환경변수를 실제 값으로 설정 (현재 기본값이 dev-demo-secret).

5) 검증
   - 배포 후 간단한 테스트용 HTML을 만들어서 실제로 <iframe src="배포URL">로
     열어봐. 그냥 URL이 열리는 것과 iframe 안에서 도는 건 다른 문제야.
   - 모바일 브라우저에서도 확인.
```

### 완료 기준

- [ ] 공개 HTTPS URL이 뜨고 로그인·컨설팅·리포트 조회가 전부 동작
- [ ] **별도 페이지의 iframe 안에서** 동일하게 동작 (세션 포함)
- [ ] 모바일 실기기에서 확인
- [ ] 서버 재시작 후에도 데모 계정과 샘플 리포트가 남아 있음

---

## 작업 E. 동선-낙상 근거 리서치

**우선순위: 2 / 예상 0.5일 / 8/10 / 기획안·포스터에도 재사용**

### 문제

현재 `rules.py`의 임계값과 권고 문구에 **근거가 없습니다.** "왜 자주 다니는 동선이
위험한가"를 문헌으로 설명할 수 있어야 컨설팅이 설득력을 가집니다.

### 지시문

```
'자주 지나다니는 동선'과 노인 낙상의 관계를 문헌으로 뒷받침하는 자료를 정리해줘.

찾아볼 방향:
- 가정 내 낙상 발생 장소 분포 (침실/거실/화장실/부엌 중 어디가 많은가)
- 야간 배뇨(nocturia)와 침실→화장실 이동 중 낙상. 우리 AI 영상에서 안방 안에
  화장실을 넣은 이유와 직결됨
- 노출(exposure) 개념: 위험 구간을 자주 지날수록 낙상 확률이 누적된다는 역학적 근거
- 보행 중 방향 전환(turning)이 직선 보행보다 낙상 위험이 높다는 연구
- 가정 내 환경 위험요인(home hazards) 개선 중재의 낙상 감소 효과 —
  Cochrane 리뷰 계열이 있을 것

산출물:
1) docs/ 아래에 근거 정리 문서 (한국어, 출처 링크 포함)
2) 그 근거를 rules.py의 판정 기준으로 어떻게 번역할지 제안.
   예: 통과 횟수 상위 구역 × 방향 전환이 일어나는 지점 × 야간 동선 여부
3) 포스터/기획안에 넣을 수 있는 3~4문장 요약

주의: 실제로 검색해서 확인한 논문만 인용해줘. 그럴듯한 제목을 지어내면
심사에서 바로 무너져.
```

---

## 작업 F. 프런트엔드 리디자인

**우선순위: 2 / 예상 2.5일 / 8/11~8/13**

### 방향

기존: 시니어 본인용 → 큰 폰트, 네오브루탈리즘(굵은 검은 테두리)
변경: **보호자용** → 세련된 대시보드 톤, 폰트 축소, 삼성생명 블루를 포인트 컬러로

### 지시문

```
webservice/frontend 를 보호자용 대시보드 컨셉으로 리디자인해줘.

1) 디자인 토큰 (src/theme.ts)
   - 현재 네오브루탈리즘(굵은 검은 outline + 고대비)을 걷어내고, 그림자와
     연한 보더 기반의 차분한 카드 스타일로.
   - 삼성생명 블루를 포인트 컬러로. 정확한 HEX는 삼성생명 공식 CI에서 확인해서
     쓰고, 어디서 가져왔는지 주석으로 남겨줘.
   - 폰트 크기를 전반적으로 축소. 시니어 대상이 아니라 보호자 대상이야.
   - edge() 헬퍼를 쓰는 곳이 여러 군데니 한 번에 정리.

2) 반응형
   - PC와 모바일 모두 지원. 브레이크포인트는 768px 기준.
   - PC에서는 여백을 살린 대시보드 레이아웃, 모바일에서는 단일 컬럼.
   - src/ui/AppShell.tsx 가 레이아웃 컨테이너이니 여기부터.
   - 전시 사이트가 iframe으로 임베드하므로 좁은 폭에서도 안 깨져야 해.

3) src/pages/Live.tsx — 실시간 감지 페이지
   - 진입 시 '카메라 연결' 버튼을 먼저 보여준다.
   - 버튼을 누르면 연결 가능한 카메라가 없다는 안내 →
     "부스에 방문하시면 실시간 체험이 가능합니다" 멘트 →
     '데모 영상 보기' 버튼으로 녹화 영상 재생.
   - 데모 영상 파일은 별도 제공 예정. 경로만 상수로 빼두면 돼.

4) src/pages/Login.tsx — 로그인
   - 온라인 관람객이 로그인 화면에서 이탈하면 체험 자체가 무산돼.
   - '체험하기(게스트로 둘러보기)' 버튼을 눈에 띄게 추가해서 데모 계정으로
     자동 로그인시켜줘. 기존 아이디/비번 입력은 남겨둬도 됨.

5) src/pages/Consulting.tsx
   - 업로드 UI에서 샘플 영상 3개(안방/거실/부엌)를 바로 고를 수 있게.
   - 분석 진행 상태 표시를 개선. 작업 A로 실제 3~5초면 끝나니까
     자연스러운 진행 표시면 충분.
```

### 완료 기준

- [ ] PC(1440px) / 태블릿 / 모바일(375px)에서 레이아웃이 깨지지 않음
- [ ] iframe으로 좁게 임베드해도 정상
- [ ] 삼성생명 블루 포인트가 일관되게 적용
- [ ] 로그인 없이 3클릭 안에 컨설팅 체험 도달 가능

---

## ✅ 진행 상황 (2026-08-06 기준)

작업 A·B·C·D·E·F·G 완료. 남은 것은 콘텐츠(영상)와 최종 점검(H)뿐입니다.

| 작업 | 상태 | 비고 |
|---|---|---|
| A 속도 최적화 | ✅ | 분 단위 → 체감 0.35초 (추론 14ms/프레임) |
| B 동선 전환 | ✅ | 통과 횟수가 값에 선형 반영됨 |
| C 경로 시각화 | ✅ | 굵은 선 + 시작/끝/회전 표식, 빨간 박스 제거 |
| D 배포 | ✅ | https://falldetectiondl-production.up.railway.app |
| E 근거 리서치 | ✅ | docs/낙상-동선-근거.md |
| F 프런트 리디자인 | ✅ | 삼성 블루, 반응형, 게스트 진입 |
| G 캐싱·부하 방어 | ✅ | 영상만 넣고 `build_samples.py` 실행하면 됨 |
| H 최종 점검 | ⬜ | 영상 통합 후 |

**은서님 대기 항목**

1. AI 할머니 영상 3개 → `webservice/frontend/public/samples/{unit,lounge,corridor}.mp4`
2. 데모 영상 → `webservice/frontend/public/demo/fall-detection-demo.mp4`
3. 삼성생명 CI 정확한 HEX → `src/theme.ts` 상단 `BRAND` 상수
4. 영상 넣은 뒤 `python3 scripts/build_samples.py --auto` 실행 후 커밋

---

## 작업 G. 결과 캐싱 + 데모 콘텐츠 통합

**우선순위: 2 / 예상 0.5일 / 8/14 / 작업 A~D 이후**

### 목적

온라인 관람객 대부분은 우리가 준비한 샘플 영상 3개를 올립니다. 결과를 미리 계산해
두고 즉시 반환하면 (1) 대기 0초, (2) 서버 부하 대폭 감소로 트래픽 리스크 해소.
**파이프라인은 그대로 살아 있고**, 처음 보는 파일은 지금처럼 실제 분석을 돕니다.

### 지시문

```
컨설팅 분석에 결과 캐시 레이어를 얹어줘.

1) 사전 계산
   - AI 생성 샘플 영상 3개(안방/거실/부엌)를 로컬에서 분석해서 리포트와
     이미지를 생성하고, webservice/consulting/ 아래에 고정 리소스로 저장.
   - 각 영상 파일의 SHA-256 해시 → 리포트 매핑을 JSON으로 관리.

2) 캐시 조회
   - routes_consulting.py 의 analyze() 에서, 업로드 파일 저장 직후 해시를 계산.
   - 알려진 해시면 백그라운드 잡을 만들지 말고 저장된 리포트 ID를 즉시 반환하는
     완료 상태 잡을 만들어. 프런트 폴링 흐름(job_id → status → report)은
     그대로 유지해야 코드 변경이 최소화돼.
   - 모르는 해시면 기존 경로 그대로 실제 분석.

3) 방어 로직
   - 업로드 파일 크기 상한 (예: 50MB). 초과 시 명확한 에러 메시지.
   - 동시에 도는 실제 분석 잡 수를 제한 (예: 2개). 초과 시 대기 안내.
     jobs.py 가 인메모리 딕셔너리 + 스레드 구조라 여기에 세마포어를 두면 돼.
   - 영상 길이 상한도 고려 (예: 60초 초과 시 앞부분만 분석).
```

---

## 작업 H. 최종 통합 점검

**우선순위: 1 / 예상 0.5일 / 8/15**

### 지시문

```
제출 전 최종 점검을 해줘. 체크리스트:

기능
- [ ] 게스트 진입 → 컨설팅 → 샘플 영상 선택 → 리포트까지 끊김 없이 동작
- [ ] 실시간 페이지의 카메라 없음 안내 + 데모 영상 재생 동작
- [ ] 리포트 이미지(경로 + 위험 구간)가 정상 렌더링

배포
- [ ] 공개 URL이 HTTPS로 열림
- [ ] 별도 페이지의 iframe 안에서 전체 플로우 동작 (세션 유지 포함)
- [ ] 모바일 실기기(iOS Safari, Android Chrome) 확인
- [ ] 서버 재시작 후 데이터 유지

부하
- [ ] 샘플 영상 업로드가 캐시로 즉시 응답하는지 확인
- [ ] 동시 요청을 여러 개 던져서 서버가 죽지 않는지 확인
- [ ] 업로드 크기/길이 제한이 실제로 걸리는지

코드
- [ ] pytest 전체 통과
- [ ] DAON_SECRET이 기본값이 아닌 실제 값인지
- [ ] 디버그 로그·주석 처리된 코드 정리
```

---

## 은서님이 직접 해야 하는 것 (Claude가 못 하는 것)

| 항목 | 마감 | 비고 |
|---|---|---|
| **AI 할머니 영상 3개 생성** | 8/9 | 안방(내부 화장실 포함) / 거실 / 부엌. **오늘 시작 필수** — 리드타임이 가장 김 |
| 실시간 감지 데모 영상 녹화 | 8/12 | 하드웨어 세팅되는 날. 화면 녹화로 충분 |
| 호스팅 계정 생성·결제 | 8/8 | Railway 또는 Render, 월 $5~7 |
| 삼성생명 CI 블루 확인 | 8/11 | 공식 색상값 |
| 부스 하드웨어 리허설 | 8/14~16 | 타일·서보 동작, `--no-serial`로 임계값 튜닝 |

---

## 일정 요약

| 날짜 | 작업 |
|---|---|
| 8/6 목 | 작업 A + B (Claude) / AI 영상 착수 (은서) |
| 8/7 금 | 작업 A·B 검증, 작업 C, 배포 준비(의존성·Dockerfile) |
| 8/8 토 | 작업 D — 배포 |
| 8/9 일 | 작업 D — iframe 검증 / AI 영상 완성 |
| 8/10 월 | 작업 E — 근거 리서치 |
| 8/11 화 | 작업 F — 토큰·반응형 |
| 8/12 수 | 작업 F — 페이지별 / 데모 영상 녹화 |
| 8/13 목 | 작업 F 마무리 |
| 8/14 금 | 작업 G — 캐싱·콘텐츠 통합 |
| 8/15 토 | 작업 H — 최종 점검·재배포 |
| 8/16 일 | **버퍼 (비워둘 것)** |
| 8/17 월 | 제출 |

### 일정이 밀리면 버리는 순서

1. 작업 C (경로 시각화) — 있으면 좋지만 없어도 동작
2. 작업 F의 PC 레이아웃 고도화 — 모바일 우선으로 최소화
3. 작업 E의 문서화 분량 — 핵심 논문 2~3편만

**절대 못 버리는 것: 작업 D(배포).** 이게 없으면 제출 자체가 성립하지 않습니다.
