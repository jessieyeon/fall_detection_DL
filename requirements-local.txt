# 로컬 개발·부스 시연용 추가 의존성.
#
#   pip install -r requirements.txt -r requirements-local.txt
#
# 여기 있는 것들은 배포 서버에서 쓰지 않습니다. 특히 face_recognition 은
# dlib 을 소스 빌드해서 CMake + C++ 툴체인을 요구하고, 컨테이너 빌드를
# 몇 분씩 잡아먹거나 실패시킵니다. 웹 서비스는 얼굴인식을 쓰지 않으므로
# (main.py --face-every 전용, 기본 꺼짐) 서버 쪽에서 뺐습니다.

# 화면 표시(cv2.imshow)가 필요한 로컬 실행용 GUI 빌드.
# requirements.txt 의 headless 를 덮어씁니다 — 둘은 같이 설치하면 안 되므로
# 로컬에서는 headless 를 먼저 지우세요:
#   pip uninstall -y opencv-python-headless && pip install -r requirements-local.txt
opencv-contrib-python==4.11.0.86

# 실시간 낙상 감지 파이프라인 (main.py)
# mediapipe 는 0.10.14 고정 — 이후 버전은 mp.solutions.pose 를 제거했다.
mediapipe==0.10.14
joblib==1.4.2
scikit-learn==1.5.1

# 아두이노 시리얼 통신 (타일/서보)
pyserial==3.5

# 얼굴 인식 — macOS 는 먼저 `brew install cmake`
face_recognition==1.3.0

# 모델 학습·평가 스크립트용
pandas==2.2.2
matplotlib==3.9.0
tabulate==0.9.0

# 테스트
pytest==8.2.0
