# 다온 프런트엔드 개발 문서

Figma 디자인(마이페이지 / 실시간 중계 / 안전 컨설팅)을 반영해 프런트엔드를 **TypeScript + 재사용 컴포넌트**로 재작성한 기록. 무엇을 만들었고, 어떤 문제가 있었고, 어떤 선택을 왜 했는지, 그 결과가 무엇인지 정리한다.

## 1. 무엇을 만들었나

- **JS → TS 전환**: 모든 프런트 소스를 `.tsx`/`.ts`로. API 응답에 도메인 타입(`User`, `Report`, `Hospital`, `Ward` …)을 부여.
- **디자인 시스템**: 네오브루탈리즘(굵은 검은 외곽선·고대비·각진 모서리)을 토큰 + 소수 컴포넌트로 정리.
- **4개 화면**: Login, My Page(어르신/보호자), Consult(컨설팅), Monitor(실시간 중계) — 하단 탭으로 이동.
- 기존 백엔드 배선(auth·survey·guardian·consulting·live WebSocket)은 **그대로 유지**하며 새 UI에 연결.

### 파일 구조
```
src/
  theme.ts          토큰(색·외곽선 헬퍼)
  index.css         전역(폰트·배경·리셋)
  ui/
    icons.tsx       인라인 SVG 아이콘(제공 3종 + 커스텀)
    AppShell.tsx    헤더 + 하단 탭 + 콘텐츠 래퍼 (모든 탭 화면 공통)
    Section.tsx     제목 + 상단 구분선 블록
    Card.tsx        흰 배경 + 검은 외곽선 박스
    Button.tsx      primary / outline / danger 변형
  api.ts            타입 붙인 API 래퍼
  pages/            Login · MyPage · Consulting · Live
  App.tsx / main.tsx
```

## 2. 문제 → 선택 → 이유 → 결과

### (a) TS 빌드: 타입 검사를 빌드에 걸까?
- **문제**: `.tsx`를 도입하면 빌드가 `tsc` 타입 검사에 막혀 실패할 수 있음. 데모 직전이라 리스크.
- **선택지**: ① `build: tsc && vite build`(엄격) ② vite/esbuild의 트랜스파일만(타입은 벗겨냄) + `tsconfig.json`은 에디터용.
- **선택**: ②. `build`는 `vite build` 그대로.
- **이유**: esbuild가 타입을 벗겨 트랜스파일하므로 **`@types` 재설치 없이도 빌드가 통과**하고 데모 속도를 지킴. 타입 안전은 에디터/`tsconfig`가 담당.
- **결과**: 45개 모듈이 73초에 빌드(느린 머신), 에러 0. 타입은 코드에 남아 유지보수에 도움.

### (b) 반복 UI: 어떻게 재사용할까?
- **문제**: 헤더·하단탭·카드·버튼·섹션 구분선이 세 화면에 반복. 그대로 두면 인라인 스타일이 수백 줄 중복.
- **선택지**: ① 페이지마다 인라인 반복 ② CSS-in-JS 라이브러리(styled-components 등) 도입 ③ 토큰 + 소수 컴포넌트.
- **선택**: ③. `theme.ts`(색·`edge()` 외곽선 헬퍼) + `AppShell/Section/Card/Button` 5개.
- **이유**: ponytail — **실제로 반복되는 것만** 컴포넌트화하고 새 의존성은 0. 기존 인라인 스타일 패턴과도 이어짐.
- **결과**: 페이지 코드가 크게 짧아짐(예: 컨설팅 화면이 스타일 잡음 없이 데이터 흐름만 남음), 세 화면의 룩이 자동으로 일치.

### (c) 디자인의 정적 콘텐츠 vs 실제 데이터
- **문제**: Figma는 "Arthur Miller", "Sarah Miller / Daughter", "City General Hospital 0.8 miles" 처럼 **가짜 정적 데이터**로 그려짐. 앱은 로그인 사용자·페어링 보호자·카카오 병원 등 실데이터.
- **선택지**: ① 디자인대로 하드코딩(예쁘지만 거짓) ② 전부 실데이터 배선 ③ 혼합.
- **선택**: ② + 백엔드가 없는 요소는 **장식으로 명시**.
- **이유**: 가짜 데이터를 진짜처럼 보이면 안 됨(정직). 실데이터가 이미 API로 있으니 배선 비용도 작음.
- **결과**: 이름·역할(로그인), 자가진단 등급·일수, 연결된 보호자, 근처 병원(카카오), 평면도, 컨설팅 리포트/히트맵, 실시간 스켈레톤이 **전부 실데이터**. 아래 "장식"만 예외.

### (d) 폰트
- **문제**: 디자인은 `Atkinson Hyperlegible Next` 지정 — Google Fonts엔 "Next"가 없음.
- **선택**: 일반 `Atkinson Hyperlegible`(400/700)을 Google Fonts로 로드 + 시스템 폴백.
- **이유**: 간결·가용성. 데모는 인터넷이 있으면 동일한 가독성 폰트.
- **결과**: 거의 동일한 룩. 디자인의 500/800 무게는 700/700으로 근사(브라우저 폴백).

### (e) 역할별(어르신/보호자) 화면 차이
- **문제**: 어르신 마이페이지(설문·평면도·병원)와 보호자 마이페이지(연결된 어르신 목록·코드 연결)는 내용이 다름.
- **선택**: `MyPage`가 role로 `SeniorHome`/`GuardianHome`을 분기, **공통 `AccountCard`는 재사용**.
- **결과**: 한 파일에서 중복 없이 두 뷰. 계정 카드/헤더/탭은 공통 컴포넌트라 자동 일치.

### (f) 실시간 캔버스가 헤드리스에서 안 그려짐
- **문제**: 백그라운드/헤드리스 탭에서는 `requestAnimationFrame`이 멈춰 스켈레톤이 안 그려짐(검증 중 발견).
- **선택**: rAF 유지(정상 렌더 방식). 헤드리스 한계는 문서화하고 **보이는 브라우저**에서 확인.
- **결과**: 실제 브라우저에서 정상. WebSocket 데이터 흐름(pose 33관절·fall 타일·reset)은 별개로 검증됨.

## 3. 장식(백엔드 없음) · 한계 — 정직하게

- **My Page**: 프로필 사진(이니셜로 대체), "로그인됨" 배지, 보호자 **CALL**(전화번호 미저장 → 동작 없음).
- **Monitor**: **Speak to Home**(음성 백채널 없음, 장식), "Living Room" 라벨(고정). **CALL EMERGENCY**는 `tel:119` 실연결.
- **Consult**: **ACKNOWLEDGE**(리포트는 이미 저장됨 → 확인용). 히트맵/소견/권고는 실제 분석 결과.
- 실시간 스트림은 로그인 사용자 전원이 같은 스트림을 봄(어르신별 분리는 향후 과제).

## 4. 실행

```bash
cd webservice/frontend && npm run build      # 코드 바뀔 때만
cd /Users/Yeon/Desktop/fall_detection_DL
python3 -m uvicorn webservice.app:app --host 0.0.0.0 --port 8000
# http://localhost:8000
```

- FastAPI가 빌드된 `dist/`를 서빙(단일 서버). `/api`·`/ws` 우선, 그 외 경로는 SPA 라우팅.
- 편집 중 즉시 반영이 필요하면 예전처럼 `npm run dev`(vite)를 별도로 써도 됨.
- 타입 검사(선택): `npx tsc --noEmit` — 빌드와 무관하게 타입만 확인.

## 5. 좋아진 점 요약

- 코드가 **짧고 일관**: 스타일은 컴포넌트/토큰에 모임, 페이지엔 데이터 흐름만.
- **타입 안전**: API 응답이 인터페이스로 고정돼 오타/모양 불일치를 에디터에서 잡음.
- **디자인 일치 + 재사용**: 새 화면을 추가해도 AppShell/Section/Card/Button만 조합하면 됨.
- **정직한 실데이터**: 시연에서 보이는 대부분이 진짜 백엔드 데이터.
